
// let gameState: "START" | "PLAYING" | "END";
// gameState = "START";

function setup(): void {
  createCanvas(1280, 704);
  textAlign(CENTER, CENTER);
}

function draw(): void {
  background(220);

}
